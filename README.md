Frontend is in react, backend is in flask. Currently there is just a users sql db on backend. Login with faizie username anad faizie password. The dashboard needs work, and also more features for the users and more stats to display.

Also, we need a system where the user can make a requst (for a flight to determine phenology) which goes to a superadmin and they approve it. 

Also how do we incorporate satellite into this? How do policy makers use it? 
